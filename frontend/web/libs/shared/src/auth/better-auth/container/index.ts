/**
 * Better Auth Container
 *
 * SERVER-ONLY: Dependency injection root.
 *
 * @module libs/shared/src/auth/better-auth/container
 */

import 'server-only';

export { AuthContainer } from './AuthContainer';
