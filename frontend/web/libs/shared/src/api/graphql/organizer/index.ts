/**
 * Organizer App API Layer
 *
 * All GraphQL operations and React hooks for the Organizer application.
 * Used by organizers for self-service operations (their own profile, events, payouts).
 *
 * Usage:
 * ```tsx
 * import * as organizerApi from '@pml.tickets/shared/api/graphql/organizer';
 * ```
 */

// TODO: Implement organizer self-service APIs
// These are for the organizer dashboard app, not admin

// Placeholder export to make this a valid module
export const ORGANIZER_API_VERSION = '1.0.0';
